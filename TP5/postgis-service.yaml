apiVersion: v1
kind: Service
metadata:
  name: postgis-service
spec:
  selector:
    app: postgis  # Doit matcher labels du pod (ajoutez label: app: postgis dans metadata.spec si besoin)
  ports:
  - port: 5432
    targetPort: 5432
